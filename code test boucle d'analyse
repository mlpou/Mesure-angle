import os
import numpy as np	 # type: ignore
import matplotlib.pyplot as plt # type: ignore
import astropy # type: ignore
import astropy.convolution as apconv # type: ignore
path = "C:\\Users\\mathi\\Rose\\Mesure-angle\\dossier_test_image\\2024-01-10-02-20-20\\"

L=[]
def convolve(imag, window):
	return apconv.convolve(imag, apconv.Box2DKernel(width=window))
def coordonnées(images):
  R=image[:,:,0]
  G=image[:,:,1]
  B=image[:,:,2]
  image2=R/3+B/3+G/3
  np.max(image2)
  np.argmax(image)
  image3=convolve(image2,3)
  np.max(image3)
  np.argmax(image3)
  len(image3[:,1])
  len(image3[1,:])
  i=np.argmax(image3)
  j=len(image2[:,1])
  k=len(image3[1,:])
  y=i//k
  x=i%k
  return [x,y]

i = 0
for entry in os.scandir(path):
	imageTmp=entry
	image=plt.imread('imageTmp')
	ListeTemp=coordonnées(image)+[i]
	L.append(ListeTemp)
	i += 1
print(L)
